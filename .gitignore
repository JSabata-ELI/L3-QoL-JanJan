# --- Build output ---
build/
dist/
__pycache__/
Shift planner/
*.pyc
*.pyo
*.pyz
*.pkg
*.toc
*.spec
*.zip
*.exe

# --- Credentials: never commit ---
# Baked notification channels (Webex bot token, mailbox password). The blob is
# only obfuscated, so anyone with the file can recover the credentials.
# It has to exist next to Diagnostic when building - just not in the repo.
Diagnostic/notify_provision.dat

# --- Runtime logs and generated output ---
*.log
Pulser Monitor/gui_out/
CSS Logger/testing/_out/
SPFE Values/testing/_*.png

# --- Lokální stav Dev Tools (každý má svůj) ---
Dev Tools/build_usage.json
Dev Tools/copy_manager_state.ini

# --- Testovací / lokální data ---
Counter of shots/data/
Time Converter/test_data/

# --- IDE ---
.vscode/
.venv/

# --- Windows ---
desktop.ini
Thumbs.db
