> **HISTORICAL (superseded 2026-08-05).** The work described here is finished and
> merged: `cpva_client.py` is in place and shared by all tabs, and the live/multi-cam
> slider work landed. Keep this file only as a record of that session's decisions —
> the current state is documented in `STRUCTURE.md` and `image_tools_structure.md`.

# HANDOFF — Image Tools optimalizace (2. 7. 2026)

Stav rozpracované práce pro pokračování na jiném PC. Kód je v working tree
(necommitnuto), synchronizuje se přes OneDrive. Branch: `Mouka`.

**Prompt pro pokračování doma:** „Pokračuj podle Image Tools/HANDOFF_02072026.md —
dokonči krok NEXT."

## Zadání (schválený plán)

Plán je v `C:\Users\jan.moucka.eli-laser\.claude\plans\koukni-se-do-image-serene-whisper.md`
(pozn.: plán soubor je lokální na pracovním PC — ale vše podstatné je shrnuto zde).

Cíle:
1. Live/online multi-cam mód slideru (`is_t.py`) extrémně pomalý na lab PC — musí
   utáhnout 12 kamer přes pomalé SMB.
2. Energie z CPVA API (slider/finder/shot finder) pomalé, občas „—", občas staré
   hodnoty → sdílený modul `cpva_client.py` (uživatel schválil).

## CO JE HOTOVO (vše zkompilováno + smoke testy prošly)

### Nový soubor: `Image Tools/cpva_client.py`
- Pool 6 keep-alive HTTPS spojení (LifoQueue), retry (transport 1×, 5xx 1×+0.3s,
  timeout/4xx → CpvaError hned), `fetch_samples/fetch_values` (vč. `.value` retry),
  `fetch_channels`, `best_shot_ns`, `nearest_sample`.
- `get_day(channel, date_key) -> DayResult(samples, status, age_s)`;
  status `ok|empty|stale|error`; error se NECACHUJE (další volání retry);
  single-flight per klíč; LRU 64; TTL 3 s jen pro dnešek.
- `warm_days`, `invalidate`, časové helpery (Prague day bounds).

### `is_t.py` (Image Slider)
- Import prolog `_import_cpva_client()` (sdílená instance přes sys.modules).
- `_pv_load_day` → wrapper nad `cpva.get_day`; `_pv_load_day_res` vrací status.
- `_pv_query_range` → `cpva.fetch_values` (propaguje CpvaError);
  `_pv_value_at_or_before` už NEcachuje None po výpadku sítě.
- `_pv_last_known_ex` vrací (hodnota, status); UI: „⟳" při error, „(old)" při stale;
  `_pv_last_known` = compat shim.
- B1: `_pv_trigger_fetch` = trailing-edge throttle 400 ms (QTimer), tělo
  přejmenováno na `_pv_trigger_fetch_now`.
- B2: `_extend_shared_timeline_from_cams` používá `self._cam_ts[ci]` místo
  přestavby O(n) pole na UI vlákně (ř. ~9760).
- B3: `_DirWatcher.ok` flag + health test `w.ok and w.is_alive()` v
  `_online_poll_multi`; adaptivní per-cam interval (konstanty ř. ~630:
  MIN 1.0 / MAX 5.0 / BACKOFF 1.5 / WATCHER 10.0 / GRACE 5 min); stagger prvních
  pollů; backoff/reset v `on_cam_found`.
- B4: `_probe_hour_folder` — wall-clock gate (UTC tuple compare) + negativní
  TTL cache 20 s; použito v `_CamPollTask` i single-cam `_PollTask`.
- B5: `poll_scan_folders` (mimo grace okno jen nejnovější hour folder);
  string filtrace přípon místo Path() v poll smyčkách; `_is_dir_quiet` (1 stat);
  existence filtr přesunut do `_CamScanTask.run` (pryč z UI vlákna);
  `load_pool` 3→8; `_cam_cache_size` = max(20, 160//n_cams) místo fixních 80;
  reuse `QPixmap.fromImage` v `_on_cam_loaded`.

### `if_t.py` (Image Finder)
- Import prolog + `CPVA_CHANNEL_MAP = cpva.CHANNEL_MAP`;
  smazán perzistentní conn + globální zámek; `_cpva_fetch_samples` = tenký wrapper.
- `_get_slider_module()` — nahrazuje DVOJÍ exec is_t.py (Save As ř. ~4919 i
  class-level GRADIENTS ř. ~5620).
- `_energy_api_for_day` → `cpva.get_day`, vrací 3-tuple (rows, per_col, had_error);
  per_col nese sidecar klíče `_ns:<col>` (bisect pole) a `_src:<col>` (api/csv).
- `_get_energy_rows_for_dt`: NECACHUJE prázdno při API chybě
  (`_energy_error_days` set → retry), dnešek TTL 30 s (`_energy_cache_time`).
- `save_primary_files_as`: energie + kopírování/anotace přesunuty do worker
  threadu (`_save_as_sig`), `cpva.warm_days` před lookupem; UI stav (annotate,
  grad_name, sel_cols) snapshot před startem; `_apply_gradient_to_image` má
  param `grad_name` + `_log_safe`.
- `_find_closest_per_col_value` používá předpočítané `_ns:` pole (konec O(files×cols×rows)).
- Tolerance: `ENERGY_MATCH_TOL_API_S = 30.0` pro API řádky (CSV zůstává 120 s)
  — použito v `_energy_parts_for_path`.

### `sf_t.py` (Shot Finder)
- Import prolog + alias mapy + `_get_slider_module()` (save cesta ř. ~2868).
- `_fetch_one_col` → `cpva.get_day` (sdílená cache ⇒ opakovaná hledání okamžitá);
  CSV fallback jen při empty; log „FETCH FAILED (will retry)" při error.
- `cpva.warm_days` před per-day smyčkou workeru (dny se stáhnou paralelně).
- `_on_selection_changed`: probe složky + `_find_image_for_ts` (SMB scandir)
  přesunuty do worker threadu `_resolve_and_load` (klik na řádek už nemrazí UI).

### `main.py`
- Pořadí načítání: `image_slider` PRVNÍ (if_t/sf_t si ho půjčí ze sys.modules
  → žádný druhý exec 13k řádků).

### Ověřeno
- `py_compile` všech 5 souborů OK.
- Smoke test: všechny 3 moduly načteny, `cpva` je JEDNA sdílená instance,
  `_get_slider_module()` vrací už načtený slider (bez re-execu), get_day
  offline → status error, error se necachuje (retry funguje).
- Helpery: nearest_sample (before/after/window), wall-clock gate (0 ms bez
  network statu), neg-cache, poll_scan_folders grace logika — vše OK.
- `build_main_window()` offscreen — 4 taby se postaví.
- Build pipeline (`Dev Tools/b_t.py` ř. 789+917): kopíruje všechny sibling
  `*.py` → `cpva_client.py` se shipne automaticky. Nic netřeba měnit.

## NEXT — co zbývá (krok 10: code review + opravy)

Běžel /code-review (8 finder agentů, část jich uživatel přerušil kvůli tokenům).
3 agenti doběhli. Nálezy k posouzení/opravě, seřazené dle důležitosti:

### Pravděpodobně reálné problémy
1. ~~cpva_client.py — single-flight waiters po chybě~~ **OPRAVENO**: `_InFlight`
   record — waiteři přebírají fetcherův DayResult (test: 5 souběžných volání
   při výpadku = 1 fetch).
2. ~~cpva_client.py — pool bez timeoutu~~ **OPRAVENO**: `_pool.get(timeout=timeout+5)`
   → CpvaError → „error"/„stale" degradace místo stallu.
3. ~~is_t.py rollover latence~~ **OPRAVENO**: `_probe_hour_folder` neaplikuje
   negativní cache na složku AKTUÁLNÍ UTC hodiny.
4. ~~if_t.py CSV sidecary~~ **OPRAVENO**: `_build_per_col_from_rows` staví
   `_ns:`/`_src:` („csv"); `_find_closest_per_col_value` už nemutuje sdílený dict.
5. **is_t.py `_pv_last_known_ex`** — extend+sort 3 dnů vzorků při každém volání;
   po warm_days při save-range (per image × channel) zbytečné CPU. Fix: cache
   merged (channel, date_key) seznamu, nebo aspoň vynechat sort (dny jdou
   chronologicky a každý je už seřazený → stačí concat).

### Menší / zvážit
6. if_t/sf_t/is_t: mrtvé importy (ssl, json, urllib.*) po migraci — uklidit.
7. sf_t `_load_api_for_day`: nepoužité `day_start/day_end/start_ns/end_ns` — smazat.
8. sf_t `_cpva_fetch_samples` wrapper bez volajících + zavádějící url — smazat.
9. if_t `_cpva_best_shot_ns` duplikuje `cpva.best_shot_ns` (drobně jiné parsování)
   — delegovat na klienta.
10. is_t `_pv_date_key/_pv_prev/next_date_key` duplikují cpva helpery — delegovat.
11. if_t `_get_energy_rows_for_dt`: 30s TTL rebuild celého per_col i když klient
    vrátil identická data — případně detekce změny přes identitu listů.

### Neproběhlé review úhly (spustit doma, pokud budou tokeny)
- line-by-line scan diffu, removed-behavior audit, cross-file tracer,
  threading/Qt audit, live-mode sémantika. Diff pro review:
  `git diff HEAD` + nový soubor `cpva_client.py`.

## Ruční ověření (až bude přístup k síti/labu)
- Slider live multi-cam ≥30 min, PV overlay: odpojit síť → „⟳", po připojení obnova.
- Image finder: View + Save As (nesmí mrazit UI), den bez dat vs. výpadek API.
- Shot finder: hledání přes více dní (2. běh okamžitý), klik na řádek nemrazí.
- Finální test na lab PC (pomalé SMB, RDCW přes UNC) — tam se potvrdí B3.

## Známé změny chování (pro operátory)
- PV overlay v live má ≤400 ms zpoždění (debounce).
- Kamera s mrtvým watcherem a bez aktivity: latence až 5 s (aktivní ~1 s).
- Dnešní energie v if_t se nově obnovují během session (dřív zamrzlé).
- API tolerance párování 30 s (CSV 120 s) — konstanta `ENERGY_MATCH_TOL_API_S`.
